import MasterMenu from './MasterMenu';

const masterMenuRoutes = [
        {
                path: '/master_menu',
                element: <MasterMenu />
        }
];

export default masterMenuRoutes;
